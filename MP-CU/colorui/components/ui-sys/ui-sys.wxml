<view class="ui-page {{sys_theme?'theme-' + sys_theme:''}} {{sys_main?'main-' + sys_main:''}} {{sys_text?'font-' + sys_text:''}}">
    <view class="page-body {{bg}} {{ui}}" style="{{styles}}">
        <view class="page-body-main">
            <app-navbar bg="{{navBg}}" title="{{title}}" wx:if="{{title}}"/>
            <slot/>
            <app-footer wx:if="{{app_footer}}"/>
            <ui-tabbar ui="shadow border-top" datas="{{tabbarData}}" wx:if="{{tabbar}}"/>
            <view class="loading-body bg-blur" wx:if="{{isLoading}}">
                <view class="_icon-loading icon-spin text-sl mb-3"/>
                <view>加载中</view>
            </view>
        </view>
        <image src="{{img}}" mode="aspectFill" class="page-img" wx:if="{{img}}"/>
    </view>
</view>