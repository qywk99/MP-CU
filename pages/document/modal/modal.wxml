<ui-sys title="模态框">
  <view class="ui-container">
    <ui-title depth="2" title="默认 系统调用" isIcon></ui-title>
    <button class="ui-btn" bindtap="show">modal</button>
  </view>
  <view class="ui-container">
    <ui-title depth="2" title="自定义" isIcon></ui-title>
    <button class="ui-btn" bindtap="showCustomizeModal" data-key='customize'>modal</button>
  </view>

  <ui-modal show="{{target == 'customize'}}"  dialog="width:auto" :option="{{false}}">
    <view class="p-4">
      <ui-loading color img="https://vkceyugu.cdn.bspapp.com/VKCEYUGU-2dd21fa9-731c-43b7-b092-2b6264029de8/ebefd126-74b4-4928-9fee-fbb2dbd5499a.png"> </ui-loading>
    </view>
  </ui-modal>


  <view class="ui-container">
    <ui-title depth="2" title="自定义2" isIcon></ui-title>
    <button class="ui-btn" bindtap="showCustomizeModal" data-key='customize2'>modal</button>
  </view>

  <ui-modal show="{{target == 'customize2'}}" bind:success='success' >
    <view class="p-5 flex-center">
      <ui-loading color img="/static/logo.png"> </ui-loading>
    </view>
  </ui-modal>

  <!-- <view class="ui-container">
			<ui-title depth="2" title="不显示取消" isIcon></ui-title>
			<button class="ui-btn" bindtap="$Modal.show('btn1')">modal</button>
			<button class="ui-btn" bindtap="show2()">系统调用</button>
		</view>

		<ui-modal name="btn1" :showCancel="false">
			<view class="p-5 flex-center">
				<ui-loading color img="/static/logo.png"> </ui-loading>
			</view>
		</ui-modal>
		
		<view class="ui-container">
			<ui-title depth="2" title="更改文字" isIcon></ui-title>
			<button class="ui-btn" bindtap="$Modal.show('text1')">modal</button>
			<button class="ui-btn" bindtap="show3()">系统调用</button>
		</view>
		
		<ui-modal name="text1" cancelText="再见" confirmText="好的">
			<view class="p-5 flex-center">
				<ui-loading color img="/static/logo.png"> </ui-loading>
			</view>
		</ui-modal> -->

  <!-- <view class="ui-container">
			<ui-title depth="2" title="透明" isIcon></ui-title>
			<button class="ui-btn" bindtap="$Modal.show('transparent1')">modal</button>
			<button class="ui-btn" bindtap="$Modal.show('transparent2')">modal</button>
		</view> -->
  <view class="ui-container">
    <ui-title depth="2" title="抽屉" isIcon></ui-title>
    <button class="ui-btn" bindtap="showCustomizeModal" data-key='drawer'>drawer</button>
    <button class="ui-btn" bindtap="showCustomizeModal" data-key='top'>top</button>
  </view>

  <ui-modal show="{{target == 'top'}}" mask="40" align="top">
    <view class="p-5 flex-center">
      <image src="/static/logo.png" mode="aspectFill"></image>
    </view>
  </ui-modal>
  <ui-modal show="{{target == 'drawer'}}" mask="40" align="bottom">
    <view class="p-5 flex-center">
      <image src="/static/logo.png" mode="aspectFill"></image>
    </view>
  </ui-modal>
  <!-- <ui-modal name="transparent1" transparent mask="40" iconCancel>
			<view class="p-5 flex-center">
				<image src="/static/logo.png" mode="aspectFill"></image>
			</view>
		</ui-modal>
		<ui-modal name="transparent2" cancelText="再见" confirmText="好的" transparent mask="40">
			<view class="p-5 flex-center">
				<image src="/static/logo.png" mode="aspectFill"></image>
			</view>
		</ui-modal> -->
</ui-sys>